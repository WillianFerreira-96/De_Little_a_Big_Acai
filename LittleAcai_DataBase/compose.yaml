services:
  mysql:
    image: mysql:latest
    container_name: LittleAcai_DataBase
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: T@is&C@r0l
    volumes:
      - /data/volume:/var/lib/mysql